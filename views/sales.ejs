<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>訪問販売の切り返し</title>
</head>
<body>
  <h1>蓄電池のネックに対する切り返しを探す</h1>
  <form action="/sales" method="POST">
    <label for="situation">お客様の状況を選択してください:</label>
    <select id="situation" name="situation" required>
      <option value="">選択してください</option>
      <option value="高い">価格が高いと言われた</option>
      <option value="必要ない">必要ないと言われた</option>
      <option value="時間がない">時間がないと言われた</option>
      <option value="他社と比較中">他社と比較中と言われた</option>
    </select><br><br>

    <button type="submit">切り返しを表示</button>
  </form>

  <h2>結果</h2>
  <p>
    <% if (response) { %>
      <strong>お客様の状況:</strong> <%= situation %><br>
      <strong>切り返し例:</strong> <%= response %>
    <% } else if (situation) { %>
      <%= situation %> に適切な切り返しが見つかりませんでした。
    <% } else { %>
      切り返しがここに表示されます。
    <% } %>
  </p>
</body>
</html>
